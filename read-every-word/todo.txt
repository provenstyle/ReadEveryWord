Wart to accept: all three calls need an authId: '' placeholder. The middleware only overwrites the field when present (if ('authId' in input)), and Ajv requires it. No security implication — the middleware replaces it with the token subject before the handler runs, and the integration tests do the same. Fixing it properly means changing the middleware and the domain request types; out of scope.