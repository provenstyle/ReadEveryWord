*.js.map
*.ts
.git*
.vscode
__azurite_db*__.json
__blobstorage__
__queuestorage__
local.settings.json
test
tsconfig.json

node_modules/azure-functions-core-tools

# Typecheck emit that lands in dist alongside the bundle. Dead weight in the
# published package; the function host only needs main.js and host.json.
apps
*.tsbuildinfo
